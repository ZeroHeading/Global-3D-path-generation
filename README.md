# Global-3D-path-generation
This is a demo for a global 3D path generation method, run the program after modifying the image path in the demo to see the corresponding global 3D path generation results.
